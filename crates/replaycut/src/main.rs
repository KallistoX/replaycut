//! replaycut - clip manager for the OBS replay buffer.
//!
//! This binary is a placeholder. The service itself (scanner, preview, share
//! pipeline, HTTP API as specified in `docs/api.md`) arrives with the 2.0 core.

fn main() {
    println!("replaycut {}", env!("CARGO_PKG_VERSION"));
    println!("The service is not implemented yet. See docs/api.md for the API contract.");
}
